"use client";

import { BarChart3, Bell, Bookmark, CalendarDays, Check, ChevronDown, ChevronRight, Compass, Filter, Home, Languages, Menu, Plus, Search, ShieldCheck, Sparkles, Star, UserCircle, WandSparkles, X } from "lucide-react";
import { useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { MboMark } from "@/components/brand/brand-marks";
import { locales, type Locale } from "@/i18n/config";
import type { DiscoveryMarket, DiscoveryPrediction, DiscoveryState, PredictionDiscoveryData } from "./types";
import styles from "./predictions.module.css";

const localeNames: Record<Locale, string> = { en: "English", es: "Espanol", fr: "Francais", de: "Deutsch", it: "Italiano", pt: "Portugues" };
const nav = [{ label: "Today", icon: Home, route: "today" }, { label: "Explore", icon: Compass, route: "predictions" }, { label: "Multi-Picks", icon: WandSparkles, route: "multi-picks" }, { label: "Performance", icon: BarChart3, route: "performance" }, { label: "Saved", icon: Bookmark, route: undefined }] as const;
const markets: Array<DiscoveryMarket | "ALL"> = ["ALL", "1X2", "MIXED", "O/U", "GG/NG", "CORNERS"];

function PredictionLine({ item, saved, added, onSave, onAdd, onOpen, locale }: { item: DiscoveryPrediction; saved: boolean; added: boolean; onSave: () => void; onAdd: () => void; onOpen: () => void; locale: Locale }) {
  return <article className={styles.predictionLine}><button className={`${styles.star} ${saved ? styles.starred : ""}`} onClick={onSave} aria-label="Save prediction"><Star size={16} fill={saved ? "currentColor" : "none"} /></button>
    <button className={styles.fixture} onClick={onOpen}><span>{item.countryCode} · {item.competition}</span><strong>{item.homeTeam} <i>vs</i> {item.awayTeam}</strong><small className={item.state === "LIVE" ? styles.live : ""}>{item.state === "LIVE" ? `${item.minute}' LIVE` : new Intl.DateTimeFormat(locale, { hour: "2-digit", minute: "2-digit" }).format(new Date(item.kickoffAt))}</small></button>
    <div className={styles.recommendation}><span>{item.market}</span><strong>{item.selection}</strong><small>{item.evidence[0]}</small></div>
    <div className={styles.oracle}><span>Oracle</span><strong>{item.oracleScore}</strong></div><div className={styles.odds}><span>Odds</span><strong>{item.odds.toFixed(2)}</strong></div>
    <button className={`${styles.add} ${added ? styles.added : ""}`} onClick={onAdd} title={added ? "Added to Multi-Pick" : "Add to Multi-Pick"}>{added ? <Check size={17} /> : <Plus size={17} />}</button>
  </article>;
}

export function PredictionsExperience({ data, locale }: { data: PredictionDiscoveryData; locale: Locale }) {
  const router = useRouter(); const oraclePick = data.items.find((item) => item.id === data.oraclePickId)!;
  const [market, setMarket] = useState<DiscoveryMarket | "ALL">("ALL"); const [state, setState] = useState<DiscoveryState | "ALL">("ALL"); const [country, setCountry] = useState("ALL"); const [minimum, setMinimum] = useState(70); const [query, setQuery] = useState(""); const [saved, setSaved] = useState(new Set(data.items.filter((item) => item.saved).map((item) => item.id))); const [added, setAdded] = useState(new Set<string>()); const [menuOpen, setMenuOpen] = useState(false);
  const countries = [...new Set(data.items.map((item) => item.country))];
  const items = useMemo(() => data.items.filter((item) => (market === "ALL" || item.market === market) && (state === "ALL" || item.state === state) && (country === "ALL" || item.country === country) && item.oracleScore >= minimum && (!query || `${item.homeTeam} ${item.awayTeam} ${item.competition} ${item.selection}`.toLowerCase().includes(query.toLowerCase()))).sort((a,b) => b.oracleScore-a.oracleScore), [country,data.items,market,minimum,query,state]);
  function toggle(setter: React.Dispatch<React.SetStateAction<Set<string>>>, id: string) { setter((current) => { const next = new Set(current); if (next.has(id)) next.delete(id); else next.add(id); return next; }); }
  return <div className={styles.app}><header className={styles.topbar}><div className={styles.topbarInner}><button className={styles.brand} onClick={() => router.push(`/${locale}/today`)}><MboMark className={styles.mark} title="MyBetOracle" /><span>MyBetOracle</span></button><label className={styles.search}><Search size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search teams, leagues or picks" /><kbd>Ctrl K</kbd></label><div className={styles.topActions}><button className={styles.pro}><Sparkles size={16} /> Oracle Pro</button><button className={styles.icon}><Bell size={18} /></button><label className={styles.language}><Languages size={17} /><select value={locale} onChange={(event) => router.push(`/${event.target.value}/predictions`)}>{locales.map((item) => <option value={item} key={item}>{localeNames[item]}</option>)}</select></label><button className={styles.icon}><UserCircle size={22} /></button><button className={styles.menu} onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X /> : <Menu />}</button></div></div></header>
    {menuOpen && <nav className={styles.mobileMenu}>{nav.map(({ label, icon: Icon, route }) => <button key={label} onClick={() => route && router.push(`/${locale}/${route}`)}><Icon size={18} />{label}</button>)}</nav>}
    <div className={styles.shell}><aside className={styles.sidebar}><nav>{nav.map(({ label, icon: Icon, route }) => <button key={label} className={label === "Explore" ? styles.navActive : ""} onClick={() => route && router.push(`/${locale}/${route}`)}><Icon size={19} />{label}</button>)}</nav><div className={styles.exploreNav}><span>EXPLORE</span><button className={styles.subActive}>Predictions</button><button onClick={() => router.push(`/${locale}/streaks`)}>Streaks</button><button>Leagues</button><button>Teams</button><button>Markets</button></div></aside>
      <main className={styles.main}><header className={styles.pageHeader}><div><span><Sparkles size={14} /> ORACLE DISCOVERY</span><h1>Predictions</h1><p>{data.analyzed} matches analyzed across {data.totalFixtures} fixtures.</p></div><button className={styles.calendar}><CalendarDays size={17} /> {new Intl.DateTimeFormat(locale, { month: "short", day: "numeric" }).format(new Date(`${data.date}T12:00:00Z`))}<ChevronDown size={15} /></button></header>
        <section className={styles.oraclePick}><div className={styles.pickIdentity}><span><Sparkles size={14} /> ORACLE PICK</span><h2>{oraclePick.selection}</h2><p>{oraclePick.homeTeam} vs {oraclePick.awayTeam} · {oraclePick.competition}</p></div><div className={styles.pickEvidence}>{oraclePick.evidence.map((entry) => <span key={entry}><ShieldCheck size={14} />{entry}</span>)}</div><div className={styles.pickNumbers}><div><span>Oracle</span><strong>{oraclePick.oracleScore}</strong></div><div><span>Odds</span><strong>{oraclePick.odds.toFixed(2)}</strong></div><button onClick={() => toggle(setAdded, oraclePick.id)}>{added.has(oraclePick.id) ? <Check size={17} /> : <Plus size={17} />}{added.has(oraclePick.id) ? "Added" : "Multi-Pick"}</button><button className={styles.openPick} onClick={() => router.push(`/${locale}/match/${oraclePick.fixtureSlug}`)} aria-label="Open match"><ChevronRight /></button></div></section>
        <section className={styles.filters}><div className={styles.marketTabs}>{markets.map((value) => <button key={value} className={market === value ? styles.filterActive : ""} onClick={() => setMarket(value)}>{value === "ALL" ? "Oracle Best" : value}</button>)}</div><div className={styles.selectors}><label><span>Country</span><select value={country} onChange={(event) => setCountry(event.target.value)}><option value="ALL">All countries</option>{countries.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>Status</span><select value={state} onChange={(event) => setState(event.target.value as DiscoveryState | "ALL")}><option value="ALL">All matches</option><option value="LIVE">Live</option><option value="UPCOMING">Upcoming</option></select></label><label><span>Oracle score</span><select value={minimum} onChange={(event) => setMinimum(Number(event.target.value))}><option value="70">70+</option><option value="80">80+</option><option value="90">90+</option></select></label><button aria-label="More filters"><Filter size={17} /></button></div></section>
        <div className={styles.listMeta}><strong>{items.length} recommended selections</strong><span>Ranked by Oracle Score</span></div><section className={styles.list}>{items.map((item) => <PredictionLine key={item.id} item={item} locale={locale} saved={saved.has(item.id)} added={added.has(item.id)} onSave={() => toggle(setSaved,item.id)} onAdd={() => toggle(setAdded,item.id)} onOpen={() => router.push(`/${locale}/match/${item.fixtureSlug}`)} />)}{items.length === 0 && <div className={styles.empty}><Search size={24} /><strong>No predictions match these filters</strong><p>Broaden the market, country or Oracle Score threshold.</p><button onClick={() => {setMarket("ALL");setState("ALL");setCountry("ALL");setMinimum(70);setQuery("");}}>Clear filters</button></div>}</section>
      </main></div>
    {added.size > 0 && <button className={styles.builder}><WandSparkles size={17} /><span>{added.size} in Multi-Pick</span><strong>Analyze <ChevronRight size={16} /></strong></button>}
    <nav className={styles.bottomNav}>{nav.map(({ label, icon: Icon, route }) => <button key={label} className={label === "Explore" ? styles.bottomActive : ""} onClick={() => route && router.push(`/${locale}/${route}`)}><Icon size={19} /><span>{label}</span></button>)}</nav>
  </div>;
}
